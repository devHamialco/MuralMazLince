/**
 * Distancia de Hamming entre dos hashes hex de 64 bits.
 * @param {string} hash1
 * @param {string} hash2
 * @returns {number}
 */
function hammingDistance(hash1, hash2) {
  return 0;
}

module.exports = {
  hammingDistance,
};
